import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  form: {
    display: "flex",
    flexDirection: "column",
  },
  reason: {
    marginBottom: "1em",
  },
  container: {
    marginTop: "4em",
  },
}));
